#include "OmniDanaCommon.h"

void DumpMessage(uint8_t* prefix, uint8_t* postfix, uint8_t* buf, size_t len)
{
  size_t i;
  printf("jepajee");
  /*Serial.print(str(prefix));
  Serial.print(len, DEC);
  Serial.print(str(postfix));

  for(i=0; i<len; i++)
  {
    Serial.print(buf[i], HEX);
  }
*/
  Serial.println(".");
}
